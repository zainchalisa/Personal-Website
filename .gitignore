# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies and build output
node_modules
dist
dist-ssr
*.tsbuildinfo

# Vite / tooling caches
.vite/
.eslintcache
.turbo/
.cache/
coverage/

# Vercel CLI (local project link — not for the repo)
.vercel/

# Secrets and local env (never commit — set in Vercel / Cloudflare for production)
.env
.env.*
!.env.example
*.local

# Cloudflare / Wrangler
.wrangler/
.dev.vars

# Local ops notes (env, deploy, asset URLs) — not for the public repo
ASSET_HOSTING.md

# Credentials (never commit)
*.pem
*.p12
credentials.json
secrets.json

# Editor / IDE
.vscode/*
!.vscode/extensions.json
.idea
.cursor/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Photon LP — full-size originals stay local; repo tracks web/ optimized assets only
src/assets/photon/baby_photos/**
!src/assets/photon/baby_photos/web/

src/assets/photon/california sunsets/**
!src/assets/photon/california sunsets/web/

src/assets/photon/family trip to england/**
!src/assets/photon/family trip to england/web/

src/assets/photon/mom&dad/**
!src/assets/photon/mom&dad/web/

src/assets/photon/redwood forest/**
!src/assets/photon/redwood forest/web/
