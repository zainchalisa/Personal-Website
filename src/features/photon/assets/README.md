# Photon landing page assets

Imported in React from `src/components/PhotonPage/`:

| File | Use |
|------|-----|
| `logo-light.png` | Header wordmark on the light LP background |
| `logo-dark.png` | Dark variant (from the macOS app) |
| `app-preview.png` | Optional marketing / preview imagery |

Favicons (fixed URLs) live in `public/photon/` — see `favicon-128.png`, etc.
